{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://paik.dev/schema/project.schema.json",
  "title": "PAIK Project",
  "description": "The entry point document for a PAIK-described project. See SPEC.md section 7.",
  "allOf": [{ "$ref": "./common.schema.json" }],
  "type": "object",
  "properties": {
    "kind": { "const": "project" },
    "description": {
      "type": "string",
      "description": "One-paragraph summary of what the project does"
    },
    "components": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Relative links to this project's component document(s) - component.md, or components/*.md for multiple"
    },
    "environments": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Relative links to this project's environment document(s)"
    }
  },
  "required": ["description", "owner", "components", "environments"],
  "unevaluatedProperties": false
}
