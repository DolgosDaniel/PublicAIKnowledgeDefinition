{
  "mcpServers": {
    "fetch": {
      "command": "uvx",
      "args": ["mcp-server-fetch"],
      "_paik": {
        "serves": ["systems/api-spec.md (spec_url, e.g. SwaggerHub)", "environments/*.md (health_endpoint, status_page)"],
        "notes": "Generic HTTP fetch server. Used as the SwaggerHub fallback since there is no single ubiquitous official SwaggerHub MCP server - swap in a dedicated one if your org standardizes on it. Also doubles as a simple way to poll health_endpoint URLs."
      }
    }
  }
}
